import{useState}from'react';import Layout from'../components/Layout';import api from'../lib/api';export default function ForgotPassword(){const[email,setEmail]=useState('');const[message,setMessage]=useState('');const submit=async(e)=>{e.preventDefault();try{const response=await api.post('/api/auth/forgot-password',{email});setMessage(response.data.resetToken?`Development reset token:${response.data.resetToken}`:response.data.message);}catch(error){setMessage(error.response?.data?.message||'Unable to process your request.');}};return<Layout><section className="container mx-auto max-w-md px-4 py-16"><p className="text-xs font-bold uppercase tracking-widest text-bnBrown">Account recovery</p><h1 className="mt-2 font-serif text-4xl font-bold">Reset your password</h1><p className="mt-3 text-stone-600">Enter your account email and we’ll send a secure reset link.</p><form onSubmit={submit}className="mt-7 rounded-2xl border border-stone-200 bg-white p-6"><label className="block text-sm font-medium">Email address<input className="field mt-2"required type="email"value={email}onChange={e=>setEmail(e.target.value)}/></label><button className="mt-5 w-full rounded-xl bg-bnBrown py-3 text-sm font-semibold text-white">Send reset link</button>{message&&<p className="mt-4 break-all text-sm text-stone-600">{message}</p>}</form></section></Layout>;}
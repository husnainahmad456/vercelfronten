import useSWR from'swr';import api from'../lib/api';import Layout from'../components/Layout';import ServiceCard from'../components/ServiceCard';import SectionHeader from'../components/SectionHeader';const fetcher=(url)=>api.get(url).then((res)=>res.data);export default function ServicesPage(){const{data}=useSWR('/api/services',fetcher);const services=data||[];return(<Layout><section className="container mx-auto px-4 py-16"><SectionHeader title="Our Services"subtitle="Comprehensive wooden services for homes and businesses."/><div className="grid gap-6 md:grid-cols-3">{services.map((service)=><ServiceCard key={service._id}service={service}/>)}</div></section></Layout>);}